spring:
  application:
    name: nitere-cloud-coupon

  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: ${spring.application.name}
        config:
          profile-separator: '-' #default value is ",", we update ‘_’
          format: YAML